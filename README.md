### task. **Расширение прототипа**

Завершите функцию конструктора `User`, чтобы она записывала параметры
конструктора как переменные экземпляра. Также завершите методы `getFullName()` и
`canVote()`.
